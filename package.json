{
  "name": "navalha-digital",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir public && cp App.tsx constants.ts index.css index.html index.tsx metadata.json types.ts public/ && cp -r components contexts hooks pages services public/"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.2.0",
    "typescript": "^5.4.5"
  }
}
